# Local-only secrets for sync scripts.
# Copy to deploy/server.secrets.local and fill in your real values.

# SSH password for ubuntu@server. Leave blank if you use SSH key login.
SSH_PASSWORD=

# Remote sudo password for the same server user.
REMOTE_SUDO_PASSWORD=
